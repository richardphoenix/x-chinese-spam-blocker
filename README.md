# X 中文 Spam 防御工具

**GitHub**: https://github.com/richardphoenix/x-chinese-spam-blocker

针对中文 X（原 Twitter）上大规模 “寻固炮” 等低俗/诈骗 spam 的社区防御方案。

针对中文 X（原 Twitter）上大规模 “寻固炮” 等低俗/诈骗 spam 的社区防御方案。

## 现状问题

大量机器人账号使用动漫头像 + 固定模板（🌸 寻固炮 🌸 点击主页）刷屏，严重破坏中文讨论环境。这些账号主要目的是引流到微信/Telegram 进行诈骗或卖服务。

## 推荐方案（已验证有效）

**GitHub 托管黑名单 + 高质量 Tampermonkey 油猴脚本**

这是 2025-2026 年社区里最成功、最可持续的模式（参考 X.com Chain Blocker、Twitter Block With Love 等成熟项目）。

### 为什么不主要靠 X API 大规模抓账号？

- X API v2 已经是 pay-per-use，搜索成本高
- 搜索结果已经被平台 spam filter 严重过滤
- 账号发现最有效的方式是 **社区举报 + 启发式规则**，而不是纯 API 爬取

## 项目结构

```
x/
├── blocklist/                    # 黑名单数据（核心）
│   ├── blocklist.json            # 主黑名单（user_id + 证据）
│   └── spam-keywords.txt         # 辅助关键词（启发式检测用）
├── userscript/                   # 油猴脚本（用户主要使用）
│   └── x-chinese-spam-blocker.user.js
├── web/                          # 管理后台（Vercel + Neon，审核提交用）
├── discovery/                    # 辅助发现工具（可选）
├── docs/
└── README.md
```

## 核心功能（脚本将提供）

- **时间线实时隐藏**：自动隐藏匹配黑名单或关键词的推文/账号（支持「寻固炮」「小狗求抱抱 / 免费曰p」和「想找会疼人的哥哥」等变种）（最推荐用法）
- **从维护者黑名单批量拉黑**：只对 Richard 审核确认过的账号开放批量拉黑（10秒/个，带队列、暂停、取消）
- **社区提交机制**：任何用户都可以一键提交疑似 spam 账号到后台审核队列，维护者在管理后台审核通过后才写入正式黑名单
- **误杀防护**：只有经过人工审核的账号才会进入可拉黑黑名单 + 支持本地白名单

## 黑名单治理模式

- 正式黑名单（`blocklist.json`）由维护者人工审核维护，所有变更均有 git 历史记录，公开透明
- 社区用户通过脚本一键提交账号，提交数据经后台 API 写入待审核队列（Neon Postgres），同一账号重复提交累加票数，不重复建行
- 维护者登录管理后台（`/admin`，GitHub OAuth）按票数排序审核 pending 列表，通过后后台自动 commit 写入 `blocklist.json`；拒绝的记录留痕隐藏
- 批量拉黑功能**只针对正式黑名单**生效，最大程度降低误杀风险

## 安装与使用

见 [docs/installation.md](./docs/installation.md)

## 如何贡献新 spam 账号

1. 在 X 页面上点击脚本控制面板里的「提交此账号到黑名单」按钮，脚本会自动将账号信息提交到后台审核队列
2. 提交进入 pending 状态，维护者在管理后台审核通过后，账号会被写入 `blocklist.json`
3. 也可直接在仓库提 PR（附上证据截图 + 链接）手动添加到 `blocklist.json`

## 开发计划

- [x] 项目骨架
- [ ] 初始黑名单 + 关键词库
- [ ] 核心 userscript（隐藏 + 拉黑 + 举报）
- [ ] 发现辅助工具
- [ ] 文档完善

## 免责与风险提示

- 批量拉黑存在账号被临时限制的风险，请务必小批量 + 带延迟操作
- 请严格区分 spam 和正常用户，误杀会损害社区

欢迎一起把中文 X 环境变好一点。
